# 主导航整体重排（分组式导航）

> 日期：2026-08-16 ｜ 范围：`web/src/components/AppShell.tsx`、`web/src/pages/ToolsPage.tsx`、`web/src/pages/CoursesPage.tsx`

## 问题
原导航是「扁平 5 标签」（工厂 / 知识图 / 课程 / 工具 / 我的），但路由里十几个页面多数没在导航暴露：
- 工厂模拟器、订单到交付散落，入口错位
- 学习路径、岗位路线几乎成孤儿页（仅工厂页内部或直链可达）
- 测验、词典、作品集完全无入口

子页散落导致新用户找不到内容。

## 方案（按用户拍板：分组侧栏 + 命名为「练习」）
把 5 个一级区改成**学习者旅程分组**，桌面侧栏直接平铺子页，移动端保持 5 个底栏、子页由各落地页进入：

| 一级区 | 子页 |
|---|---|
| 工厂 | 全景 / 模拟器 / 订单到交付 |
| 课程 | 课程 / 学习路径 / 岗位路线 |
| 知识图 | （单一页面） |
| 练习 | SQL 沙盒 / 测验 / 词典 |
| 我的 | 个人中心 / 作品集 |

## 改动文件
1. `AppShell.tsx` — `NAV` 增加 `children` 与 `match`；侧栏改为 `.nav-group` + `.nav-sublist`（复用既有样式），移动端底栏不变仍 5 项。
2. `ToolsPage.tsx` — 去掉「工厂模拟器」（已归工厂），改为 SQL 沙盒 + 测验 + 词典，标题「工具」→「练习」。
3. `CoursesPage.tsx` — 补「岗位路线 / 全部学习路径」直达链接，移动端底栏无子页也能进。

## 验证
- `tsc -p web/tsconfig.json --noEmit` → EXIT 0
- `vite build`（web）→ 构建成功

## 待用户处理
- 未提交 / 未部署。需要可 `git commit` + 推送 / `wrangler deploy`。
- 真机核验：桌面侧栏子页平铺、移动端底栏 5 项 + 落地页子入口。
