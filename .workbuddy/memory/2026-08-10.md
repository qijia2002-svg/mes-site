# 2026-08-10 工作日志

## 工厂页重做对齐课程页视觉 + 首次真正部署

**根因复盘**：此前多轮"改了没生效"的真正原因是——导航重构（5 tab）、词典扩充、初学者路径等改动**全部停留在未提交的工作区**，从未 `git commit` + `wrangler deploy`，线上跑的仍是旧 bundle。截图里工厂页底栏只有 2 tab、无面包屑，正是旧版残留。

**本轮改动（已提交 `99dea05`，已部署 `ea56294e`）**：
- `web/src/pages/FactoryPage.tsx`：工厂页头部由自定义 `.fh` 重样式改为课程页同款 `.page-head`/`.page-title`/`.page-sub`/`.page-head-actions`/`.card`/`.pill` 共享视觉语言。
- `web/src/features/factory/FactoryJourney.tsx`：旅程各站头改为 `.section`/`.section-head`/`.section-title`/`.row-meta`（标题按阶段色着色），与课程页"章节测试 3 道题"区块观感一致；去掉站点图标，留呼吸感。
- 导航重构（5 tab：工厂/词典/课程/工具/我的）、工具枢纽页、离线通用词典（188→255）、初学者路径一并随本次提交落地。

**验证（真机/线上证据）**：
- 构建产物 `index-oBBFmAKK.js` 含 NAV 五 tab 标签 + `page-title`/`section-title`/`fh-kicker`/`fh-prog` 类，旧 `.fh` 类清零。
- 线上 `https://mes-site.qijia2002.workers.dev`（+3 自定义域名）主包同 hash，含 5 tab 与新样式类；`/factory`、`/tools` 均 200。

**部署链路（稳定可用）**：`npm run build --workspace web -- --outDir ../.tmp-build` → `cd worker && npx wrangler deploy --assets ../.tmp-build`。绕 `worker/dist` 的 Windows EPERM 字体锁；wrangler 已本机登录（`whoami` 确认）。

**待办**：用户需在手机复验底栏 5 tab、工厂页清爽头部/站头、面包屑（WORKBENCH > 工厂）。下一步用户另行安排。

## 手机端横向滚动综合治理（已部署 `102a1c5b`）

**用户痛点**：手机端要左右滑、卡片没堆叠、文字被截断、流程圆圈连接线错位、字号大看不全。

**改动**：
- `web/src/styles.css`：① 全局兜底 `html,body{overflow-x:hidden;max-width:100%}` + `*{box-sizing:border-box}` + `img/svg/pre/table{max-width:100%}`，任何宽元素都撑不出横向滚动条。② `@media(max-width:640px)` 重定义 `--text-*`（24→20/20→17/18→16/15→14/13→13/12→11）缩小字号；`.tools-grid` 改 `1fr`；`.card/.section/.fj-node/.ml-btn/.tools-card` 等 `max-width:100%` 左右留边不溢出。
- `web/src/features/factory/MainlineStepper.tsx`：手机端 `@media(max-width:768px)` 从「横滑 scroll-snap 6 chip」改为「左侧竖向时间线」——`.ml-rail{display:block}` + `.ml-rail::before` 连接线 + `.ml-item::before` 圆点（done/current 着色），彻底消除横向滚动，与旅程时间线视觉一致。
- `web/src/features/factory/FactoryJourney.tsx`：`.fj-node .ftitle` 去掉 `nowrap+ellipsis` 改 `white-space:normal;word-break:break-word` 自动换行；`.fone` 加 `word-break` 防溢出。

**验证**：构建产物确认 `overflow-x:hidden` 进主 CSS、`scroll-snap-type:x mandatory` 全消失、`ml-rail::before`/`ml-item::before` 时间线规则与 `1.25rem` 字号 token 进包。部署 `102a1c5b`。待 Playwright 375×812 视口跑横向溢出检测 + 截图存证。
