-- ============================================================================
-- 种子：微练习分级提示 practice_hints（micro 9401-9412 全量 L1/L2/L3）
-- ----------------------------------------------------------------------------
-- 配套：seed-learn-redesign-content.sql（该文件 e) 段原本含 9401/9405/9407 的
--       L1+L2 三条，已移至本文件统一 ownership；本文件是 micro 提示的唯一来源。
-- 前置：schema-learn-redesign.sql 已建 practice_hints 表。
--
-- 设计约束（勿违，ADR-019）：
--   H1  level 1 指方向 / 2 给关键点 / 3 给做法，**仍不得包含可直接提交的答案**。
--   H2  micro / quiz 的 L3 不点破最终选项，只把判据摆到眼前（见下方逐条）。
--   H3  不给 sim 配提示：12 个节点当前没有任何 sim 挂载，配了是查不到目标的
--       幽灵提示（内容侧 C3：没内容不出行）。
--   H4  body_md 不得出现 ASCII 单引号（破坏 SQL 字面量）；代码词用反引号，
--       中文用「」。
--   H5  显式覆盖：先 DELETE 本段目标行再 INSERT，保证可重跑无 UNIQUE 冲突。
--
-- D1 Free 约束：本文件写入 36 行，远低于每日 10 万行配额。
-- ============================================================================

PRAGMA foreign_keys = OFF;

DELETE FROM practice_hints
 WHERE target_type = 'micro' AND target_id BETWEEN 9401 AND 9412;

PRAGMA foreign_keys = ON;

INSERT INTO practice_hints (target_type, target_id, level, body_md) VALUES

-- 9401 cust-order · 急单判断（pick 多选）
('micro', 9401, 1, '急单要两个字段同时成立：`review_status` 还是 `pending`，`due_date` 又已经逼近。只看交期会把已评审过的正常单也算进来。'),
('micro', 9401, 2, '`review_status = pending` 的是没评审过；`due_date` 离今天 2026-08-08 还有几天，越近越急。已经 `approved` 的不算急单。'),
('micro', 9401, 3, '逐张看两个字段：哪几张是 pending 且交期在 08-15 之前（含更近的 08-12 / 08-13）。不要凭「交期早」就选——`approved` 的两张交期再早也不在该选的集合里。'),

-- 9402 order-review · 接了没排（pick 多选）
('micro', 9402, 1, '两个字段一起看：`review_status` 是 `approved`，`plan_status` 是 `none`。少看一个就会捞错单。'),
('micro', 9402, 2, '`approved` 加 `none` 才是「接了没排」。已经 `planned` 的在计划里，`rejected` 的根本没接，都不算漏排。'),
('micro', 9402, 3, '逐张确认 `plan_status` 是不是 `none`，同时 `review_status` 是不是 `approved`。已经 `planned` 的单、`rejected` 的单，都不在该选的集合里。'),

-- 9403 mps · 四步排序（order）
('micro', 9403, 1, '想清楚顺序：先算需求，再看产能，然后冻结，最后下工单。'),
('micro', 9403, 2, '看产能（capacity）在冻结（freeze）之前——产能没确认不能先下工单；冻结在下达工单（wo）之前——计划没锁死，工单下去会被改。'),
('micro', 9403, 3, '四个动作里，哪个是「汇总订单算出总需求量」排第一，哪个是「下达生产工单」排最后。中间两个谁先谁后，看「先确认产能够不够，再锁死计划不让改」。'),

-- 9404 mrp · 数字连线（match）
('micro', 9404, 1, '先把「应发量 62」怎么来的算一遍：60 × 1 × (1 + 0.03)。'),
('micro', 9404, 2, '公式：应发量 = 单件用量 × 计划量 × (1 + 损耗率)。60 是计划量，1 是单件用量，0.03 是损耗率，62 是算出来的结果。'),
('micro', 9404, 3, '左边四个数里，哪个是「计划做多少台」、哪个是「每台用多少」、哪个是「损耗比例」、哪个是「算出的应发量」。把公式套进去，剩下两个自然对上。'),

-- 9405 purchase · 逾期未到（pick 多选）
('micro', 9405, 1, '「逾期」和「没到货」是两个条件，要同时成立。'),
('micro', 9405, 2, '承诺日 `promise_date` 早于今天 2026-08-08 是第一条；`arrive_date` 还是空是第二条。到货了但数量不足属于短交，是另一类问题。'),
('micro', 9405, 3, '逐张看：承诺日过没过今天？货到了没？`PO-20260722-01` 承诺 08-11 还没到今天，不算逾期；`PO-20260718-01` 已到货，是短交不是逾期。'),

-- 9406 bom-route · 产品连物料（match）
('micro', 9406, 1, '注意接线端子在两个产品里都出现，但每台用量不同——先对用量，再对产品。'),
('micro', 9406, 2, '减速机每台用 4 套轴承（用量最大）；伺服电机每台 1 件定子组件；PLC 用 12 个端子；变频器用 8 个端子。'),
('micro', 9406, 3, '把「每台用量最大的那种料」作为判据：减速机的最大用量料是轴承，伺服电机是定子组件，PLC 和变频器的都是端子但数量不同（12 vs 8）。先定每个产品的「最大用量料」，再回看右边四个选项里的数量。'),

-- 9407 picking · 齐套判断（pick 单选）
('micro', 9407, 1, '先想清楚齐套到底是百分比，还是是非题。'),
('micro', 9407, 2, '产线要装出成品，需要的每一样料都得到位。差 22 件定子组件，机器转不起来——这种情况下「65%」这个数字对现场有任何意义吗？'),
('micro', 9407, 3, '判据只有一个：十样料是不是全都发齐了。只要有一样没发齐（这里差 22 件），这张工单就是开不了工，不存在「按比例折算齐套率」这种说法。'),

-- 9408 dispatch · 派工卡点（pick 单选）
('micro', 9408, 1, '派工要落到具体设备上。先查这个车间有几台设备、状态分别是什么。'),
('micro', 9408, 2, '二号车间只有 `EQ-02` 一台设备，它的状态是停机——指令下到了车间，但车间没有可用设备，工单就悬着。'),
('micro', 9408, 3, '`released` 只表示「指令已下到车间」，能不能真正开工要看该车间有没有 status 为运行 的设备。选项里只有「设备停机」能让 `released` 的工单卡住。'),

-- 9409 shopfloor · 报工顺序（order）
('micro', 9409, 1, '先想清楚哪一步在往表里写行、哪一步是写完之后自动算的。'),
('micro', 9409, 2, '扫条码确认给哪张工单报工 → 录入合格/不良数 → 提交后 `production_records` 多一行 → 工单的 `qty_done` 由这些报工累加。'),
('micro', 9409, 3, '四个动作里，「扫条码」一定是第一步（不扫不知道报给谁），「qty_done 累加」一定最后（是前面提交的结果，不是人手填的）。中间两步先录数再提交。'),

-- 9410 qc · 质量动作排序（order）
('micro', 9410, 1, '按时间轴想：换型后第一件在什么时候？入库前那道在什么时候？处置动作又排在哪？'),
('micro', 9410, 2, '首检在最前（换型后第一件），巡检在过程中，终检在入库前，判不合格之后的返工/让步/报废处置排在最后。'),
('micro', 9410, 3, '四个动作里，只有「首检」发生在生产一开始、「处置」发生在检出不合格之后——它是结果不是过程。剩下两个按「过程中抽」和「入库前全检」排。'),

-- 9411 stock-in · 入库按哪个数（pick 单选）
('micro', 9411, 1, '先别急着选一个数。想想：这两个数为什么会不一样？入库之后还查得回来吗？'),
('micro', 9411, 2, '报工合格数（45）和工单完工数（40）对不上，说明中间少了一笔账——可能是漏报、返工、或报工没滚进工单。'),
('micro', 9411, 3, '判据是「两个数对不上就先查清楚，不入库」。入库会把错账带进库存、后面盘点全乱。所以正确动作是查清差异，不是选 45、选 40 或取平均。'),

-- 9412 shipping · 哪批逾期（pick 单选）
('micro', 9412, 1, '把每一批的交期和实际发运日期一行行比。分批发货，问题几乎都出在最后一批。'),
('micro', 9412, 2, '`SH-02-1` 实发 08-12 ≤ 交期 08-13，准时；`SH-02-2` 实发 08-18 > 交期 08-15，晚了 3 天。'),
('micro', 9412, 3, '判据是「实际发运日 vs 承诺交期」，谁晚于交期谁出问题。整单准时率看尾批，首批准时不能救尾批逾期。');
