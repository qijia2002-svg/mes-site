# MES 学习平台 · 职业体系审查报告（2026-08-03）

> 范围：知识内容、路线体系、UI（重点 /roadmap）。结论基于线上 D1 数据 + 前端源码扫描 + 部署后验证。

## 一、体系现状（线上实测）

| 维度 | 数值 | 说明 |
|------|------|------|
| 能力路线 tracks | 11 条（已隐藏 1 条草稿） | 原 12，programming-dev 因全空置 draft |
| 等级层级 track_levels | 36（12×3） | — |
| 章节 chapters | 130 | 平均 1424 字，min 198 / max 4611，均为真实内容 |
| 主题 topics | 32 | — |
| 数据完整性 | 0 空正文 / 0 孤儿链接 / 0 坏图标 | 健康 |

## 二、本次修复的缺陷（已部署，worker 版本 41ad68b8）

1. **track 11/12 占位 summary（27–28 字）** → 补全为真实描述（WMS/APS/BI 三系统协同；精益+IE 价值流主线）。
2. **track 10 二开与编程基础：3 个层级 0 章节（完全空壳）** → `status='draft'`，从路线图隐藏。脑库无对应目录，无法从知识库补。
3. **/roadmap 布局重叠（你反馈的"职业路径挡到职业说明"）**
   - 根因：`.rm-stage-cell`（说明列）桌面端非 sticky；`.rm-scroll` 可横向滚动，滚动时节点（z-index:1）盖住说明列。
   - 修复：`styles.roadmap.css` 桌面端 `.rm-stage-cell` → `position:sticky; left:0; z-index:2`。
   - 验证：部署 CSS 已确认含该规则。
4. **track 12 图标 `workflow` 未注册** → `trackIcons.ts` ALIAS 补 `'workflow':'routing'`（此前退化成 Route 错误图标）。track 9/10 的 `clipboard-list`/`code` 已由既有 ALIAS 正确解析，无误。
5. **P0 合规扫描全绿**：emoji 功能图标 0、紫粉渐变 0、硬编码色值（除 #fff/#000）0。

## 三、遗留待你决策的缺口

| 项 | 现状 | 建议 |
|----|------|------|
| 脑库 `AI` 目录 | 平台无对应路线，完全缺失 | 是否新增"AI 与制造业"路线？ |
| 脑库 `质量` 目录 | 未独立成路线，可能散落在 MES 质量模块 | 确认是否需独立路线 |
| 脑库 `PowerBI` 目录 | 已并入 wms-aps-bi（BI） | 无需新增 |
| 多条 L3（高级）为空 | erp/mes/sql/plc/barcode/lean-ie 的 L3 无章节 | MVP 常见，后续内容扩充 |
| track 10 二开 | 已隐藏 | 提供内容或我生成后可恢复 |

## 四、操作记录

- D1 更新：tracks 11/12 summary、track 10 status=draft（已验证生效）
- 前端：修复 sticky 布局 + workflow 图标别名 → `vite build` → `wrangler deploy`
- 线上验证：`/api/v1/tracks` 返回 11 条（programming-dev 已排除），11/12 含新 summary；部署 CSS 含 sticky 规则
