# 模拟器「跑班动态播放」改造 · 概览

> 日期：2026-08-13 · 关联：改造模拟器（用户选择方向「跑班动态播放」）

## 改了什么

给工厂模拟器（4 道工序：下料→机加工→组装→检验）的「活体产线」加了一条**跑班播放条**，把原来只给稳态终态读数的产线，变成「点一下就能看见这一班怎么跑完」的活体动画。

### 核心能力
- **跑班 / 暂停 / 继续 / 重跑 / 重置**：播放条含主按钮 + 重置，进度条显示「X.X / 8 小时」班次进度。
- **在制品随班次慢慢堆起来**：补上了它自注的「没演示随时间堆积」短板。末端吞吐仪表从 0 涨到 M1，在制品堆积块从 0 涨到 M3。
- **瓶颈拥堵 + 下游饥饿可视化**：
  - 瓶颈（机加工）前面那段连线变红、光点变密 → 前面积压；
  - 瓶颈之后的连线变淡、光点变稀 → 下游在干等；
  - 工序卡片实时标「最卡 / 干等料出不去 / 等料停工 / 前面积压 / 运转中」+「本班已做 X 件」。

### 实现要点（不动核心数学）
- **动画模型 `cumAt(caps, Q, t)`**：把 `simCalc` 的稳态产能展开成「一个班次内逐分钟累计」的纯串联队列——第 i 道工序累计完成 = `min(上游累计, 本工序单班产能 × 班次进度)`。终态 `t=480` 严格收敛到 `simCalc` 的 `produced`，因此动画与右侧稳态读数**完全一致、不造假**。
- `requestAnimationFrame` 推进班次时间（约 14 秒跑完一班），改任意参数即自动重开这一班，保证动画与当前配置一致。
- 纯前端动画驱动，**未修改 `simCalc.ts` 任何逻辑**。

### 文件改动
- `web/src/features/factory-sim/FactorySimPage.tsx`：新增播放状态机 + RAF 循环 + `cumAt`/`connClass` 辅助 + 播放条 UI + 每工序实时状态。
- `web/src/features/factory-sim/FactorySimPage.css`：播放条、实时状态胶囊、拥堵/饥饿连线样式（复用 `--accent`/`--danger`/`--border`/`--ease-standard` 等令牌，无 emoji、无紫粉渐变、无硬编码色）。
- 同步更新「简化假设」折叠文案：原「没演示它随时间慢慢堆起来的过程」→ 改为指向「跑班」播放。

## 验证
- `tsc -p web/tsconfig.json --noEmit` 通过。
- `npm run deploy` 成功（先 `mv worker/dist/assets worker/dist/assets_old` 规避 EPERM 覆盖写）。
- 线上 `https://shuojia.qzz.io` → HTTP 200；产线 chunk `FactorySimPage-CYD-S6IN.js` 已含 `跑班`/`sim-playbar`/`is-congested`/`本班已做`。

## 待办 / 备注
- GitHub 推送仍被旧 token 吊销阻断（无凭据），本次改动**仅本地提交未推送**，需用户在本机 `git push` 或提供新 token。
- 本次会话此前 3 个提交（nav 重构 8c5a101、清理+文档 2efa710、工厂全景唤醒 2099dc2）同样未推送。
