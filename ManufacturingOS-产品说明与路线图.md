# Manufacturing OS — 产品愿景 × 当前项目对比 & 落地路线图

> 本文档对比你提供的《Manufacturing OS 完整特征汇总》与当前 `mes-learning-platform` 实际代码，给出「哪里不一样 / 现在该加什么 / 技术决策点」，供立项排期使用。

## 0. 一句话结论

你这份 spec 把平台从「MES 学习站」升级为「Manufacturing OS｜交互式制造业数字化学习平台」。核心差异一句话：

**现状 = 内容 + SQL 实操 + 进度记录（看 + 练），愿景 = 在前面基础上多出「可操作的工厂仿真沙盒」（做）。**

这是 spec 反复强调的「王牌特色 / 核心竞争力」，也是当前项目最大的空白。其余差异多为「已有雏形待补齐」（个人中心、Obsidian 同步、路由级懒加载）或「技术栈命名不一致」（TanStack Router / Cloudflare Pages）。

---

## 1. 逐项对比（spec 六大块 → 现状 → 差距）

| spec 章节 | spec 要求 | 当前项目现状 | 差距 |
|---|---|---|---|
| 一、核心产品理念 | 拒绝纯文字，动手搭建 → 可视化运行 → 即时反馈（对标 Scratch / TurboWarp） | 以「读章节 + 写 SQL」为主，无可视化可操作组件 | ❌ 缺「做」的环节（最大短板） |
| 二.1 交互式沙盒 | Canvas 虚拟车间、自定义工艺路线/工序/工单/BOM、拖拽组装、仿真运行、出错可视化 | 无任何 Canvas / 仿真；仅 `schema.sql` 预留了 `fault_scenarios` / `block_solutions` 表 | ❌ 完全缺失（需新建仿真引擎 + 画布 + 数据模型） |
| 二.2 系统化学习 | 生产模式(ETO/MTO/MTS/离散vs流程)、ERP(BOM/MRP/工单/采购/计划)、SQL | 已有 `learning_paths` / `topics` / `chapters`（路线图导入） + SQL 工作台 + quiz | ⚠️ 生产模式 / ERP 主线内容缺失，需补课程线 |
| 二.3 在线实训工作台 | Web 端 SQL 运行环境 + 制造业真实业务表 + 即写即看 | ✅ 已实现（sql.js WASM + MES 样例库 + 判题） | ✓ 已具备 |
| 二.4 内容管理 | Obsidian 双向同步、Markdown 渲染、自动侧栏/目录/面包屑 | Markdown 章节经 markdown-it 渲染（已有）；无 Obsidian 同步管线；面包屑 `CrumbProvider` 已有但未接内容树 | ⚠️ 缺 Obsidian 同步 + 内容树自动生成 |
| 二.5 个人中心 & 成长 | 进度持久化(D1)、任务清单、阶段闯关、作品集（求职展示） | ✅ 进度持久化(D1, anon_id) + 首页进度仪表盘已上线；❌ 无任务清单 / 闯关 / 作品集 | ⚠️ 缺成长体系与作品集 |
| 三、交互 & 界面 | 响应式 PC+移动、加载/异常/重试兜底、重组件路由懒加载、分层导航 | ✅ 响应式(v2 移动端 tabbar)、✅ 状态块兜底；❌ 未做路由级代码分割 | ⚠️ 需补路由级 lazy |
| 四、技术架构 | React19 + Vite + **TanStack Router**（类型安全嵌套路由）、原生 Canvas、D1、**Cloudflare Pages** 部署 | React19 + Vite ✅；**react-router-dom 7（声明式，非 TanStack Router）**；原生 Canvas 待引入；**Workers + Static Assets 部署（非 Pages）** | ⚠️ 路由库与部署形态与 spec 不一致（见 §3 决策点） |
| 五、差异化 | 可视化可操作工厂沙盒 + 贴合实施岗求职 + 理论/SQL/仿真三位一体 + 可当作品集 | 理论 + SQL 已具备；沙盒 / 作品集缺失 | ❌ 缺沙盒与作品集 |
| 六、长期拓展 | 真实案例库、需求调研模拟、多工厂模型切换 | — | 📌 依赖沙盒落地后扩展 |

---

## 2. 现在要加什么（优先级）

### P0 — MES 工厂仿真沙盒（王牌，必须先做）
- 原生 Canvas 虚拟车间舞台（不引第三方库）
- 数据模型：工艺路线 / 工序 / 工单 / BOM 结构（取材奥来电器断路器真实流程）
- 拖拽式组件组装生产链路（类似积木）
- 仿真运行：工单下发 → 报工 → 质检 → 追溯 → 物料流转
- 错误注入与可视化：复用已预留的 `fault_scenarios` 表，模拟流程出错带来的业务问题
- 参数可改、反复试错、零成本模拟工厂痛点
- **路由级懒加载**（640KB+ 仿真引擎不进首屏）
- 落库：仿真场景另存 D1（可选，v1 可纯前端 state）

### P1 — 个人中心 & 成长体系
- 任务清单 / 阶段闯关模式（复用 `learning_paths` + 进度）
- 作品集板块：存项目方案 / MES 需求文档 / 实施笔记，可求职展示
- 进度已有，补「闯关」与「作品集」两层

### P1 — Obsidian / Markdown 内容同步管线
- 本地 Obsidian 编写 → 站点同步展示
- 自动侧边栏 + 目录(TOC) + 面包屑（`CrumbProvider` 已在，接内容树即可）
- 卡片式页面布局

### P1 — 生产模式 / ERP 主线课程线
- ETO / MTO / MTS / 离散 vs 流程制造对比
- BOM / MRP / 工单 / 采购 / 计划 业务逻辑
- 以新增 `learning_paths` + `topics` 形式落地

### P2 — 长期拓展（依赖沙盒）
- 多工厂模型切换（阀门厂 / 电机厂 / 电器装配厂）
- 真实 OEM 断路器 MES/ERP 项目案例库
- 需求调研模拟 / 流程梳理实训

---

## 3. 技术栈差异 & 需要你拍板的决策点

1. **路由库**：spec 写 TanStack Router（类型安全嵌套路由）；现状 `react-router-dom` 7。迁移成本中等（路由文件重写 + 类型生成），收益是类型安全嵌套路由。**建议**：新功能若做沙盒独立大模块，可在该模块内局部采用，或整体迁移——**需你定**。
2. **部署形态**：spec 写 Cloudflare Pages；现状 **Workers + Static Assets**（SPA 回退已配置）。现状已能同源托管 SPA + API，迁移 Pages 收益有限且要改绑定，**建议维持 Workers**，除非你明确要求 Pages。
3. **Canvas 库**：spec 明确原生 Canvas API，✓ 与「不引多余依赖」原则一致，无需引 Konva / fabric。
4. **前端框架 / SQL 沙箱**：React19 + Vite + sql.js 一致 ✓。

---

## 4. 建议的下一步

- 立即开工 **P0 仿真沙盒**，按项目标准 SOP 走：
  - Phase 0 需求澄清（你确认仿真颗粒度 / 首期工厂模型）
  - Phase 1 并行调研（PM 竞品 + 架构师技术选型 + 设计师交互稿）
  - 三文档确认 → Spec → 设计 → 开发 → 测试部署
- 我可以先出一份《仿真沙盒 PRD + 技术选型（Canvas 状态机 / 数据模型 / 懒加载）》给你过目，再正式开工。
- P1 的「个人中心 / 作品集」「Obsidian 同步」「生产模式课程线」可并行排期，但**建议沙盒先行**（它是差异化核心，也撑起作品集素材）。

---

## 5. 当前已具备（避免重复建设）

- 前端骨架 / 设计系统(token) / 响应式 / 状态兜底 ✅
- 浏览器端 SQL 工作台（sql.js WASM）✅
- 内容模型 + 进度持久化(D1) + 首页进度仪表盘 ✅
- 后端内核（洋葱管道 / 统一响应 / D1 / DO 限流）✅
- `schema.sql` 已预留 `fault_scenarios` / `block_solutions`（沙盒「出错可视化」可直接复用）✅
