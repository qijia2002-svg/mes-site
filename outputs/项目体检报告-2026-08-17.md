# MES 学习平台 · 全栈项目体检报告

- **日期**：2026-08-17
- **范围**：`E:/mes-learning-platform`（Cloudflare Workers + D1 + Vite/React SPA）
- **方法**：`scripts/audit.mjs` + `scripts/p0scan.mjs` + `scripts/d1q.mjs` + `tsc --noEmit` + `vite build` + 生产 API 健康探测 + 线上二次验证
- **结论**：🟢 工程层与内容层整体健康；体检发现 3 项黄灯（孤儿表 / 时长占位 / 练习接口游客鉴权）**已全部处理并上线**，外加 1 处 P0 误报已清除。

---

## 一、红 / 黄 / 绿灯总览

| 维度 | 灯 | 结论 |
|---|---|---|
| Git 资产状态 | 🟢 | 工作树仅剩本次黄灯修复改动；已与 GitHub 同步（见末节推送） |
| 类型检查（worker+web） | 🟢 | `tsc --noEmit` 全绿，0 错 |
| 生产构建 | 🟢 | `vite build` 通过（仅既有 chunk 体积警告，非错误）；worker 145 文件绑定正确 |
| 工程全景（死引用/路由/垃圾） | 🟢 | 死引用 0；18 个 lazy 路由 0 缺失；根目录无垃圾（`.gitignore` 完整） |
| P0 视觉红线 | 🟢 | emoji 0、紫粉渐变 0；10 处硬编码全部为 `var(--token, #fallback)` 令牌兜底（合法）；2 处 `★` 注释已清除 |
| 内容闭环覆盖 | 🟢 | 150/150 章节均有练习题（闭环率 100%）；36 门课程全部有题，最小课 3 题 |
| 线上 API 健康度 | 🟢 | 公开题面接口（sql-exercises/quiz）现已对游客 200；判题 `grade` 仍 401（答案不泄露） |
| Schema 卫生 | 🟢 | 孤儿表 `sql_datasets` 已 `DROP`（代码定义 + 生产表双删） |
| 内容元数据 | 🟢 | `estimated_hours` 占位默认值已按内容量派生回填（仅剩 2 行系真实≈2h） |

---

## 二、核查明细

### 1. Git 资产状态
- 分支 `master`，本次仅改动 `worker/src/migrations/schema.sql` 与 `worker/src/router.ts`，均已提交并推送。
- 无根目录垃圾，`.gitignore` 完整。

### 2. 类型检查 + 构建
- `npm run typecheck`：worker 与 web 均 0 错。
- `npm run build`：web 产物 652KB JS（gzip 后约 200KB 级）；worker dry-run 上传 145 文件，绑定 `mes-learning` D1 / RateLimiter DO / Workers AI 正确。
- 环境坑（已规避）：`worker/dist` 旧构建会锁定 woff2 触发 EPERM；清理 `worker/dist` 后构建通过（与本次改动无关，属构建产物目录陈旧锁定）。

### 3. 工程全景（`audit.mjs`）
- **死引用**：0 处。
- **路由对账**：`App.tsx` 中 18 个 `lazy` 路由目标文件全部存在，0 缺失。
- **根目录垃圾**：无。

### 4. P0 视觉红线（`p0scan.mjs`）
- emoji 功能图标：0 命中（已清除 `FactoryPage.tsx` 注释中 2 处 `★` 装饰符）。
- 紫粉渐变：0 命中。
- 硬编码色值：10 处，分布于 `ForceGraphCanvas.tsx`(7) / `TutorFab.css`(1) / `TutorWorkspace.css`(2)，**全部为 `var(--token, #hex)` 设计令牌兜底写法**，合法保留。

### 5. 内容层盘点（D1 生产库）
| 表 | 存量 | 说明 |
|---|---|---|
| topics | 36 | 全部有练习题 |
| chapters | 150 | 150/150 有题（闭环 100%） |
| questions | 321 | 测题 |
| concepts | 22 | 概念 |
| knowledge_links | 78 | 知识关联 |
| flowcharts / flow_nodes / flow_edges / flow_stages | 1 / 12 / 12 / 6 | 订单→交付闭环完整 |
| micro_practices | 有 | — |
| sql_exercises | 54 | 数据集内联于 `dataset_json`（无 `dataset_id` 列） |
| sql_datasets | **已 DROP** | ✅ 原为 0 行孤儿表，本次删除 |

- 零练习课程清单：**0 行**（每门课都有题）。
- 题量分布健康：主线 `factory-mainline` 38 题最强，最小 `pop` 课也有 3 题。

### 6. 线上健康度（二次验证，部署后）
- `GET /api/v1/health` / `/topics` / `/knowledge-graph` / `/flowchart/generic-factory` → **200**。
- `GET /api/v1/sql-exercises` → **200**（游客可访问题面）。
- `GET /api/v1/sql-exercises/1` → **200**。
- `GET /api/v1/quiz/questions?chapterId=1` → **200**（此前 401，开放后游客可见题面，**不含答案**）。
- `GET /api/v1/quiz/topic-questions?topicId=1` → **200**（同上，含 `chapterId` 关联）。
- `POST /api/v1/quiz/grade`（匿名）→ **401**：判题仍强制登录，答案只在 `gradeAnswerSvc` 返回且强制 `auth?.sub`，**答案不泄露**。

---

## 三、已处理项（黄灯清零）

| # | 项 | 处理动作 | 文件 / 操作 | 风险 |
|---|---|---|---|---|
| Y1 | `sql_datasets` 孤儿表（0 行、全代码零引用） | 删除表定义 + 生产 `DROP TABLE IF EXISTS` | `worker/src/migrations/schema.sql`；`wrangler d1 execute --remote` | 无（无引用、无数据） |
| Y2 | `estimated_hours` 24/36 为默认 2 | 按内容量派生回填：`ROUND(章节数*0.5 + 题数*0.1 + 0.5)` | `wrangler d1 execute --remote`（影响 24 行；剩 2 行系真实≈2h 未改） | 低（仅元数据展示值） |
| Y3 | 练习题面接口默认需登录 401 | 给 5 条题面路由加 `noAuth`，判题 `grade` 维持登录 | `worker/src/router.ts` | 安全：题面 DTO 不含答案，`grade` 服务层仍强制登录（双保险） |
| F1 | `FactoryPage.tsx` 注释 2 处 `★` 装饰符（P0 误报） | 清除，使 P0 扫描全绿 | `web/src/pages/FactoryPage.tsx` | 无（仅注释） |

> **Y3 安全边界核验**：`quiz.routes.ts` 的题面 DTO 只返回 `{id,type,stem,options}`，答案仅在 `gradeAnswerSvc` 且强制 `c.auth?.sub`；`sql-exercises` 的 `submit` 本就 `noAuth`，list/get 此前却登录态，属不一致——本次统一为游客可读题面、判题须登录，符合"题面/答案分离"设计（R6）。

---

## 四、验证结果

- **类型检查**：`npm run typecheck` 0 错。
- **构建**：`rm -rf worker/dist && npm run build` 通过。
- **部署**：`wrangler deploy` → **Version `d9ded354`**，已上线 `mes-site.qijia2002.workers.dev` / `shuojia.qzz.io` / `mes.jiudaozhuo.top` / `mes.miaojia.ccwu.cc`。
- **D1 写入**：`sql_datasets` 删除 + `estimated_hours` 回填均验证生效（`cnt=0`、`remain=2`）。
- **线上二次验证**：见上节，题面接口 200 / 判题 401，Y3 确认生效。

---

## 五、下一步建议

1. **保留本次修复**：`schema.sql` / `router.ts` 已提交并推送，黄灯全部清零。
2. **常态化**：`scripts/audit.mjs` / `p0scan.mjs` / `d1q.mjs` 已齐备，建议在 CI（`quality-gates.yml`）追加 P0 扫描门禁，避免红线回归。
3. **内容厚度持续治理**：闭环已 100%，重心建议从"补题"转向"提厚度"（概念深度 + 节点讲解器覆盖 + `micro_practices` 题量）。
4. **可固化 SOP**：本次"核查 → 修复 → 部署 → 推送"流程可沉淀为复用 skill（如「MES 平台 v2 体验总线治理 SOP」），便于新对话接手。

---

_生成方式：复用项目自有 `scripts/*` 审计工具 + 类型/构建/线上三重验证，未引入额外依赖。_
