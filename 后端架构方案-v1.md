# 后端架构方案 v1

> 上游依据：《生产规范-SKILL-v2.md》（唯一事实来源）
> 定位：在 v2 规范的**免费额度硬约束**下，把"一个 Worker + 一个 D1 + 一个 DO"重构成一套**可扩展、可降级、可观测**的后端架构
> 日期：2026-07-31

---

## §A0 本方案要解决的四个问题

| 问题 | 现状风险 | 本方案对策 |
| --- | --- | --- |
| **扩展性**：新增学习主题/模块需要改后端代码 | 规范 §2.5 只说"topics 表驱动"，未定义后端如何不改代码支撑新模块 | §A5 模块注册表 + config schema 白名单 + 内容契约版本化 |
| **稳定性**：D1 单库单线程，一条慢查询拖垮全站 | 规范 §16.6 只给了约束，没给**运行时防护** | §A4 查询预算守卫 + §A7 降级三级 + 慢查询熔断 |
| **额度耗尽**：10 万请求/天、10 万行写入/天，一次爬虫即打满 | 规范只在设计期约束，运行期无兜底 | §A6 Cache API 只读缓存 + DO 令牌桶 + 进度批量合并 |
| **可维护性**：`/api/*` 全塞进一个 fetch handler | 无 | §A2 分层 + §A3 洋葱中间件管道 |

**不做的事**（明确划界，避免过度设计）：不引入消息队列、不做 CQRS、不做多区域复制、不做服务拆分。单人项目 + 免费套餐下，微服务化是负资产。**本方案是"单体 Worker 内部的模块化"，不是分布式架构。**

---

## §A1 架构全景

```
                       ┌──────────────────────────────┐
   Browser             │   Cloudflare Edge (免费版)     │
 ┌──────────┐          │                              │
 │ React 19 │──HTTPS──▶│  WAF 1 条规则 (/api/* 兜底)   │
 │ sql.js   │          │            │                 │
 │ 沙箱     │          │            ▼                 │
 └──────────┘          │   ┌─────────────────────┐    │
                       │   │  Worker (单实例)     │    │
                       │   │  ├ ASSETS  静态资源  │    │
                       │   │  └ /api/* 路由        │    │
                       │   │     ↓ 中间件管道      │    │
                       │   │  ┌───────────────┐   │    │
                       │   │  │ trace→cors→   │   │    │
                       │   │  │ auth→limit→   │   │    │
                       │   │  │ validate→svc  │   │    │
                       │   │  └───────────────┘   │    │
                       │   └──┬──────┬───────┬────┘    │
                       │      │      │       │         │
                       │      ▼      ▼       ▼         │
                       │  ┌──────┐ ┌────┐ ┌────────┐   │
                       │  │  D1  │ │ DO │ │ Cache  │   │
                       │  │500MB │ │限流│ │  API   │   │
                       │  │单线程│ │锁定│ │只读缓存│   │
                       │  └──────┘ └────┘ └────────┘   │
                       └──────────────────────────────┘
```

**关键判断：Cache API 是本次重构最高杠杆的一笔。** Workers 免费版 `caches.default` 不计入 D1 额度、不占 DO 请求、命中时 CPU 近乎为零。理论内容（章节 MD、题库、topics 树）读多写少，全部走缓存后，D1 读请求可降一个数量级，直接缓解"单库单线程"这根最紧的弦。规范 v2 中未提及，是本方案的主要增量。

---

## §A2 分层与目录结构

严格四层，依赖单向向下，禁止跨层反向依赖。

```
src/worker/
├── index.ts                  # 入口：ASSETS 分流 + /api/* 交给 router
├── router.ts                 # 路由表（静态映射，零正则回溯）
├── env.d.ts                  # Env 绑定类型
│
├── core/                     # ① 内核层（与业务无关，可整体复制到下个项目）
│   ├── pipeline.ts           #   洋葱中间件 compose
│   ├── context.ts            #   Ctx 定义与构造
│   ├── response.ts           #   统一响应 { code, data, msg, traceId }
│   ├── errors.ts             #   AppError 体系与错误码表
│   ├── logger.ts             #   结构化 JSON 日志
│   ├── crypto.ts             #   HMAC 签名/校验、常量时间比较
│   └── cache.ts              #   Cache API 封装 + 单飞（single-flight）
│
├── middleware/               # ② 管道层
│   ├── trace.ts              #   traceId + 耗时统计
│   ├── security.ts           #   CSP / Origin 校验 / 安全响应头
│   ├── auth.ts               #   会话解析 + /admin/* 鉴权
│   ├── ratelimit.ts          #   调 RateLimiter DO
│   ├── validate.ts           #   入参 schema 校验
│   └── errorBoundary.ts      #   兜底捕获，永不裸抛
│
├── modules/                  # ③ 领域层（按业务模块切，新增主题不动此层）
│   ├── auth/                 #   登录、登出、改密
│   ├── content/              #   topics / chapters / 内容读取（只读，走缓存）
│   ├── quiz/                 #   题目下发、答题提交、错题本
│   ├── sqlex/                #   SQL 题目与数据集下发、结果上报
│   ├── practice/             #   积木 / 沙盘 记录与判定规则下发
│   ├── progress/             #   进度批量提交、stats_daily 聚合
│   └── admin/                #   后台 CRUD、Excel 分片导入、导出
│
├── data/                     # ④ 数据层
│   ├── db.ts                 #   DbSession：预算守卫 + 慢查询日志 + 重试
│   ├── repositories/         #   每表一个 repo，只出现 prepare-bind
│   └── sql/                  #   复杂只读语句常量（便于 EXPLAIN 校验）
│
├── do/
│   └── RateLimiter.ts        # Durable Object：令牌桶 + 登录阶梯锁定
│
└── registry/                 # 扩展点（见 §A5）
    ├── modules.ts            #   模块能力注册表
    └── demoSchemas.ts        #   ::: demo 容器 config 白名单 schema
```

**规则**
1. `modules/*` 只能依赖 `core/`、`data/`、`registry/`；**禁止** `data/` 反向 import `modules/`。
2. `core/` 零业务语义，可原样复用。
3. 一个模块 = 一个 `routes.ts` + 一个 `service.ts`，路由声明与业务实现分离。

---

## §A3 请求生命周期与中间件管道

### A3.1 洋葱模型（零依赖自研，约 60 行）

**技术选型说明**：不引入 Hono / itty-router。理由：规范 §9.1"不擅自更换技术栈"，且路由规模 < 40 条，静态映射表比通用路由库更快、脚本更小（免费版 3MB gzip 上限虽宽裕，但 10ms CPU 下每一次正则回溯都是成本）。若后续路由超过 80 条，再评估引入 Hono（约 20KB gzip），届时需走变更决议。

```ts
// core/context.ts
export interface Ctx {
  req: Request;
  env: Env;
  exec: ExecutionContext;
  url: URL;
  params: Record<string, string>;
  traceId: string;
  startedAt: number;
  db: DbSession;                       // 带预算守卫的 D1 包装
  auth?: { sub: string; ver: number }; // 已鉴权的管理员会话
  log: Logger;
}

export type Handler = (c: Ctx) => Promise<Response>;
export type Middleware = (c: Ctx, next: Handler) => Promise<Response>;

// core/pipeline.ts
export function compose(mws: Middleware[], handler: Handler): Handler {
  return mws.reduceRight<Handler>(
    (next, mw) => (c) => mw(c, next),
    handler,
  );
}
```

### A3.2 固定执行顺序（不可调整）

| # | 中间件 | 职责 | 失败返回 |
| --- | --- | --- | --- |
| 1 | `errorBoundary` | 最外层兜底，捕获一切异常转统一响应 | 500 |
| 2 | `trace` | 生成 `traceId`（`crypto.randomUUID()`）、记录 `durationMs` | — |
| 3 | `security` | 写操作校验 `Origin`；响应注入 CSP 与安全头 | 403 |
| 4 | `auth` | 解析签名 Cookie；`/api/admin/*` 强制校验 | 401 |
| 5 | `ratelimit` | 敏感端点调 DO 令牌桶 | 429 |
| 6 | `validate` | schema 校验入参（体积、类型、枚举） | 400 |
| 7 | `handler` | 领域逻辑 | — |

**顺序理由**：鉴权在限流之前，是为了让**已登录管理员**与匿名请求走不同的桶键（`admin:{sub}` vs `ip:{ip}`），避免管理员被匿名流量的桶挤爆；同时未通过鉴权的请求根本不必消耗 DO 请求额度——但登录接口例外，它必须**先限流后验密**，因此登录路由单独配置 `[trace, security, ratelimit, validate, loginHandler]`。

### A3.3 统一响应与错误码

```ts
// core/response.ts
export const ok = <T>(c: Ctx, data: T, status = 200) =>
  json(c, { code: 0, data, msg: 'ok', traceId: c.traceId }, status);

export const fail = (c: Ctx, e: AppError) =>
  json(c, { code: e.code, data: null, msg: e.publicMsg, traceId: c.traceId }, e.status);
```

错误码分区（`code` 为业务码，HTTP 状态码如实返回，符合 §11.3）：

| 区段 | 含义 | 示例 |
| --- | --- | --- |
| `0` | 成功 | — |
| `1xxx` | 入参/校验 | `1001` 参数缺失、`1002` 体积超限、`1003` schema 未通过白名单 |
| `2xxx` | 认证授权 | `2001` 未登录、`2002` token 过期、`2003` token 版本失效、`2004` Origin 非法 |
| `3xxx` | 限流锁定 | `3001` 触发令牌桶、`3002` 登录阶梯锁定（响应带 `retryAfterMs`） |
| `4xxx` | 业务规则 | `4001` 内容为草稿不可见、`4002` 题目不存在 |
| `5xxx` | 依赖故障 | `5001` D1 overloaded、`5002` 查询预算超限、`5003` DO 不可达 |
| `9xxx` | 未分类内部错误 | `9000` |

**红线**：`publicMsg` 只允许固定文案，**禁止把 D1 原始错误信息回传前端**（会泄露表名列名）。原始错误只进结构化日志，前端凭 `traceId` 报障。

---

## §A4 数据访问层：把 D1 硬约束变成运行时护栏

规范 §16 给的是"设计时要注意"，落到工程上必须变成**代码强制**，否则一定会被违反。

### A4.1 DbSession：查询预算守卫

```ts
// data/db.ts
const MAX_STMT_PER_REQUEST = 40;   // 免费版红线 50，留 20% 余量
const SLOW_QUERY_MS = 100;

export class DbSession {
  #used = 0;
  constructor(private db: D1Database, private log: Logger) {}

  get used() { return this.#used; }

  #charge(n: number) {
    this.#used += n;
    if (this.#used > MAX_STMT_PER_REQUEST) {
      throw new AppError(5002, 500, '请求过于复杂', { used: this.#used });
    }
  }

  async first<T>(sql: string, ...binds: unknown[]): Promise<T | null> {
    this.#charge(1);
    return this.#run(() => this.db.prepare(sql).bind(...binds).first<T>(), sql);
  }

  async all<T>(sql: string, ...binds: unknown[]): Promise<T[]> {
    this.#charge(1);
    const r = await this.#run(() => this.db.prepare(sql).bind(...binds).all<T>(), sql);
    return r.results;
  }

  async batch(stmts: D1PreparedStatement[]) {
    this.#charge(stmts.length);
    return this.#run(() => this.db.batch(stmts), `batch(${stmts.length})`);
  }

  async #run<T>(fn: () => Promise<T>, label: string): Promise<T> {
    const t0 = Date.now();
    try {
      return await fn();
    } catch (err) {
      throw mapD1Error(err, label);          // overloaded → 5001
    } finally {
      const ms = Date.now() - t0;
      if (ms > SLOW_QUERY_MS) this.log.warn({ msg: 'slow_query', label, ms });
    }
  }
}
```

**收益**：任何人写出"循环里查库"的代码，在 Preview 环境第一次跑就会被 `5002` 打断，而不是上线后把 Worker 调用打到 50 次触发平台报错。这是**用架构约束替代人工纪律**。

### A4.2 Repository 契约

```ts
// data/repositories/chapter.repo.ts
export const chapterRepo = {
  listByTopic: (db: DbSession, topicId: number, cursor = 0) =>
    db.all<ChapterRow>(
      `SELECT id, title, sort, updated_at FROM chapters
       WHERE topic_id = ?1 AND status = 'published' AND id > ?2
       ORDER BY id LIMIT 100`,
      topicId, cursor,
    ),
  // ...
};
```

**强制项**
- 只出现 prepare-bind，**任何字符串拼接 SQL 在 code review 直接驳回**（§9.6）。
- 单次返回 ≤ 100 行（§16.3）；列表接口一律 cursor 分页，`WHERE id > ? LIMIT 100`。
- 每个 repo 文件头部注释写明**依赖的索引名**，migration 中必须存在同名索引；CI 阶段用 `EXPLAIN QUERY PLAN` 校验无 `SCAN TABLE`。

### A4.3 写额度守恒（每天 10 万行）

三条落地规则：

1. **进度类数据不逐次落库**。前端 debounce 合并，`POST /api/v1/progress/batch` 一次提交 ≤ 50 条事件，后端合并为 ≤ 3 条 SQL。
2. **幂等去重**：每条事件带客户端生成的 `event_id`（UUID），`progress_events` 表 `event_id` 建 UNIQUE 索引，写入用 `INSERT OR IGNORE`。网络重试不会产生重复行。
3. **写时预聚合**（§2.1 / §10 阶段 4），用 UPSERT 一条语句完成，绝不 SELECT-then-UPDATE：

```sql
INSERT INTO stats_daily (user_id, day, sql_done, quiz_done, practice_done, updated_at)
VALUES (?1, ?2, ?3, ?4, ?5, ?6)
ON CONFLICT(user_id, day) DO UPDATE SET
  sql_done      = sql_done      + excluded.sql_done,
  quiz_done     = quiz_done     + excluded.quiz_done,
  practice_done = practice_done + excluded.practice_done,
  updated_at    = excluded.updated_at;
```

**额度测算**：单人日均 200 次交互 → 合并后约 20 次请求 × 3 行 = 60 行/天，占日额度 0.06%。即使未来放开到 100 名学员，也只占 6%。写额度不构成瓶颈，**前提是 debounce 机制不被绕过**——因此进度写接口只暴露 `batch` 一个端点，不提供单条写入 API，从接口设计上堵死。

---

## §A5 扩展性设计：新增主题不改后端代码

这是"更好的扩展性"的核心答案。目标：**新增一个 WMS 运维主题，后端零代码改动，只做数据录入。**

### A5.1 三层扩展契约

```
①  topics 表          → 决定"有哪些主题"          （纯数据）
②  modules 注册表     → 决定"主题能开哪些模块"     （代码，枚举有限）
③  demoSchemas 白名单 → 决定"内容里能挂哪些组件"   （代码 + schema）
```

只有当出现**全新交互形态**（例如新增"网络拓扑排障"组件）时才动 ②③；新增主题、新增章节、新增题目全部落在 ①，走后台录入。

### A5.2 模块注册表

```ts
// registry/modules.ts
export const MODULE_REGISTRY = {
  theory:     { table: 'chapters',       cacheTtl: 300, public: true  },
  quiz:       { table: 'questions',      cacheTtl: 120, public: true  },
  sql:        { table: 'sql_exercises',  cacheTtl: 300, public: true  },
  simulation: { table: 'fault_scenarios',cacheTtl: 300, public: true,
                variants: ['factory', 'blocks'] },
} as const;

export type ModuleKey = keyof typeof MODULE_REGISTRY;
```

`topics.modules` 字段存 JSON 数组（如 `["theory","sql","quiz"]`），后端下发主题时按注册表过滤并生成前台可用模块列表。**未在注册表中的 key 一律丢弃**，防止脏数据让前端崩溃。

### A5.3 `::: demo` config 白名单校验（§11.5 / §24.2 的落地）

规范只说"必须走白名单校验"，未给机制。这里定义**声明式 schema + 极简校验器**（不引入 zod，避免包体与 CPU 开销）：

```ts
// registry/demoSchemas.ts
type Field =
  | { t: 'enum'; values: readonly string[]; required?: boolean }
  | { t: 'int'; min: number; max: number; required?: boolean }
  | { t: 'bool'; required?: boolean }
  | { t: 'id'; required?: boolean };            // 仅 [A-Za-z0-9_-]{1,32}

export const DEMO_SCHEMAS = {
  factory: {
    scenarioId: { t: 'id', required: true },
    theme:      { t: 'enum', values: ['day', 'night', 'auto'] },
    perspective:{ t: 'bool' },
  },
  sql: {
    exerciseId: { t: 'id', required: true },
    showHint:   { t: 'bool' },
  },
  blocks: {
    solutionId: { t: 'id', required: true },
    maxSteps:   { t: 'int', min: 1, max: 20 },
  },
} satisfies Record<string, Record<string, Field>>;
```

校验时机：**写入时校验（发布接口），而非读取时校验**。原因是 10ms CPU 预算下，读接口是热路径，校验成本必须前置到低频的后台发布操作。未知键**静默丢弃**并记 warn 日志，不报错——否则一个手滑的多余参数会让整篇文章无法发布。

### A5.4 API 版本化

全部路由前缀 `/api/v1/`。破坏性变更时并行提供 `/api/v2/`，`v1` 保留至少一个发布周期。**这是低成本的**：路由表加一层映射即可，但能保证前端缓存中的旧 JS 在部署瞬间不会全部 500。

### A5.5 内容契约版本

`content_meta` 增加 `schema_version INTEGER`。前端渲染器按版本选择解析策略，允许老文章不迁移。**避免"改一次容器语法就要全量刷库"**。

---

## §A6 缓存架构（本方案最大增量）

### A6.1 分层

| 层 | 载体 | 对象 | TTL | 失效方式 |
| --- | --- | --- | --- | --- |
| L1 | isolate 模块级变量 | `platform_config`、`token_version` | 60s | 到期自然失效 |
| L2 | `caches.default` | 只读内容 API 响应 | 300s | URL 版本号切换 |
| L3 | 浏览器 TanStack Query | 客户端状态 | 按查询 | 乐观更新 + invalidate |

### A6.2 L2 失效策略：版本号即缓存键

免费版没有 cache tag purge，所以**不做"删缓存"，改做"换键"**：

```ts
// 内容发布时递增 platform_config.content_version
// 只读接口的缓存键带上版本号
const key = new Request(
  `https://cache.internal/v1/topics/${id}?cv=${contentVersion}`,
  { method: 'GET' },
);
```

发布后 `content_version` 从 7 变 8，所有旧键自然不再被命中，300s 后被平台回收。**零 purge 调用、零额外额度消耗、强一致地反映发布结果。**

### A6.3 单飞（single-flight）防击穿

缓存失效瞬间，若多个请求同时打到 D1，单线程 D1 会排队。isolate 内用 promise 去重：

```ts
// core/cache.ts
const inflight = new Map<string, Promise<unknown>>();

export async function cachedJson<T>(
  key: string, ttl: number, loader: () => Promise<T>,
): Promise<T> {
  const cache = caches.default;
  const req = new Request(`https://c.internal/${key}`);
  const hit = await cache.match(req);
  if (hit) return hit.json<T>();

  if (inflight.has(key)) return inflight.get(key) as Promise<T>;

  const p = (async () => {
    const data = await loader();
    const res = new Response(JSON.stringify(data), {
      headers: { 'content-type': 'application/json',
                 'cache-control': `max-age=${ttl}` },
    });
    await cache.put(req, res.clone());
    return data;
  })().finally(() => inflight.delete(key));

  inflight.set(key, p);
  return p;
}
```

> 注意：单飞只在**同一 isolate 内**生效，跨边缘节点无效。对本项目（个人 + 面试演示流量）足够；不要为此引入分布式锁，那会把 DO 额度和复杂度都推高。

### A6.4 哪些接口可缓存

| 可缓存（L2） | 不可缓存 |
| --- | --- |
| `GET /api/v1/topics` | 所有 `/api/v1/admin/*` |
| `GET /api/v1/topics/:id/chapters` | `GET /api/v1/progress/*`（用户态） |
| `GET /api/v1/chapters/:id`（正文 MD） | 所有写接口 |
| `GET /api/v1/sql/exercises/:id`（题面 + 数据集） | `POST /api/v1/auth/*` |
| `GET /api/v1/quiz/:chapterId`（**不含答案**） | |

**安全红线**：题目下发接口的响应体**绝不包含标准答案**。SQL 判题在浏览器 WASM 内完成（§4.2），需要标准答案 SQL —— 这是既定设计，但必须与"选择题答案"区分：选择题答案留服务端校验，`POST /api/v1/quiz/submit` 返回判定结果。否则缓存的 JSON 里带答案，等于公开答案。

---

## §A7 稳定性设计

### A7.1 三级降级

| 级别 | 触发条件 | 行为 |
| --- | --- | --- |
| **L0 正常** | — | 全功能 |
| **L1 只读降级** | D1 连续 3 次返回 overloaded（isolate 内计数，30s 窗口） | 写接口直接返回 `503 + code 5001 + retryAfterMs`；读接口强制走 L2 缓存，允许返回 stale 数据并在响应头标 `x-stale: 1` |
| **L2 静态降级** | D1 完全不可用 | 只读接口返回缓存；无缓存则返回 `{ code: 5001, data: null }`，前端 ErrorBoundary（§15.7）显示"内容加载失败，请重试"，**不白屏** |

降级状态存在 isolate 模块级变量中，**不写 D1、不占 DO**。isolate 回收即自动复位，这个"自愈"特性正好符合免费套餐的调度模型。

### A7.2 重试策略

```ts
// 仅对幂等只读查询重试；写操作绝不自动重试（避免重复写）
async function retryRead<T>(fn: () => Promise<T>, max = 2): Promise<T> {
  for (let i = 0; ; i++) {
    try { return await fn(); }
    catch (e) {
      if (i >= max || !isOverloaded(e)) throw e;
      await sleep(30 * 2 ** i + Math.random() * 20);   // 30ms / 60ms + 抖动
    }
  }
}
```

**关键认知**：Workers 的 10ms 限制是 **CPU 时间**，不含 I/O 等待。因此重试的 sleep 与 D1 往返不消耗 CPU 预算，这条重试是安全的。但**总墙钟时间**仍受 Worker 生命周期约束，故最多重试 2 次。

### A7.3 幂等性矩阵

| 接口 | 幂等键 | 机制 |
| --- | --- | --- |
| `POST /progress/batch` | `event_id` | UNIQUE + `INSERT OR IGNORE` |
| `POST /quiz/submit` | `submission_id` | UNIQUE，重复提交返回首次结果 |
| `POST /admin/import/chunk` | `importId + chunkIndex` | 分片表记录已完成分片，重传跳过 |
| `POST /auth/login` | 不幂等 | 由限流保护 |

Excel 导入的幂等尤其重要：规范 §12 要求 100 行/片串行 POST，网络抖动导致某片重传时，若不幂等就会产生重复题目。

### A7.4 Excel 分片导入的事务边界

```
前端解析 → 按 100 行切片 → 串行 POST（携带 importId, chunkIndex, total）
   ↓ 每片
后端：① 校验 chunkIndex 未处理过        (1 条查询)
      ② 8 列 → 12 行/条 INSERT → 9 条语句
      ③ 标记分片完成                    (1 条)
      → db.batch([②③]) 原子提交 ≈ 11 条语句，远低于 40 预算
   ↓ 全部完成
前端调 POST /admin/import/commit → 将 status 从 staging 改为 published
```

**分片表**：`import_chunks(import_id, chunk_index, rows, done_at, PRIMARY KEY(import_id, chunk_index))`。这是对规范 §12 的补强——原文说了"分批原子性"，但没有跨请求的**整体一致性**方案。用 `staging → commit` 两阶段，避免导入中断留下半份题库。

---

## §A8 认证与限流

### A8.1 会话：HMAC 无状态 + 版本吊销（§11.4 落地）

```ts
// payload: base64url(JSON) . base64url(HMAC-SHA256)
type SessionPayload = { sub: string; ver: number; exp: number; iat: number };
```

- 算法：WebCrypto `HMAC / SHA-256`，密钥来自 `env.SESSION_SECRET`（`wrangler secret put`）。
- 比较：**必须用常量时间比较**，禁止 `===` 比字符串（时序侧信道）。
- Cookie：`HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=28800`。
- 吊销：改密时递增 `platform_config.token_version`。

### A8.2 token_version 的读取成本问题（规范未覆盖）

若每个 `/admin/*` 请求都查一次 D1 拿 `token_version`，后台操作会**成倍消耗 D1 额度**，且在 D1 排队时后台直接不可用。

**方案：isolate 级 60s TTL 缓存 + 改密即时生效补偿**

```ts
let verCache: { v: number; exp: number } | null = null;

async function getTokenVersion(c: Ctx): Promise<number> {
  const now = Date.now();
  if (verCache && verCache.exp > now) return verCache.v;
  const row = await c.db.first<{ v: number }>(
    `SELECT CAST(value AS INTEGER) AS v FROM platform_config WHERE key = 'token_version'`);
  verCache = { v: row?.v ?? 1, exp: now + 60_000 };
  return verCache.v;
}
```

**权衡说明**：最坏情况下旧 token 在改密后仍可用 60 秒。对单管理员的个人项目，这个窗口完全可接受，换来的是每分钟最多 1 次 D1 查询而非每请求 1 次。若未来必须做到即时吊销，正解是把 `token_version` 移入 RateLimiter DO（强一致、不吃 D1），但会 1:1 消耗 DO 请求额度——**当前不做，记入 §A11 演进项**。

### A8.3 RateLimiter DO 设计

```ts
// do/RateLimiter.ts
export class RateLimiter {
  private tokens = new Map<string, { t: number; ts: number }>();

  constructor(private state: DurableObjectState) {}

  async fetch(req: Request): Promise<Response> {
    const { key, capacity, refillPerSec, cost } = await req.json<ConsumeReq>();
    const now = Date.now();

    let b = this.tokens.get(key);
    if (!b) b = (await this.state.storage.get(key)) ?? { t: capacity, ts: now };

    // 惰性回填
    b.t = Math.min(capacity, b.t + ((now - b.ts) / 1000) * refillPerSec);
    b.ts = now;

    const allowed = b.t >= cost;
    if (allowed) b.t -= cost;

    this.tokens.set(key, b);
    await this.state.storage.put(key, b);
    await this.state.storage.setAlarm(now + 3_600_000);   // 1h 后清理冷键

    return Response.json({
      allowed,
      retryAfterMs: allowed ? 0 : Math.ceil(((cost - b.t) / refillPerSec) * 1000),
    });
  }

  async alarm() {
    // 清理长时间未使用的桶，控制 DO 存储在 5GB 免费额度内（实际用量极小）
  }
}
```

**桶配置**

| 端点 | 桶键 | 容量 | 回填 |
| --- | --- | --- | --- |
| `POST /auth/login` | `login:{user}` + `login:ip:{ip}` 双桶 | 5 | 0.2/s |
| 写接口 `/progress/batch`、`/quiz/submit` | `w:{ip}` | 30 | 1/s |
| `/admin/import/chunk` | `imp:{sub}` | 60 | 2/s |
| 其余只读接口 | **不挂 DO** | — | 靠 WAF + Cache |

**登录阶梯锁定**（§17.2）：账号+IP 双维度，5 次锁 1 分钟 / 10 次锁 15 分钟。实现上把锁定状态与令牌桶放在同一个 DO 实例（`idFromName('login')`），一次 DO 往返同时完成计数、锁定判断与回填，**不额外增加 DO 请求数**。

**DO 额度守恒**：只读接口不挂限流是刻意设计。DO 免费额度 10 万请求/天，若全站挂载，DO 请求数会 ≈ Worker 请求数，两个额度同时被消耗，等于把日承载能力砍半。只读侧交给 Cache API + WAF 那 1 条规则兜底。

### A8.4 wrangler.toml 补正

规范 §6 的配置**缺少 DO migrations 声明**，缺了它 `wrangler deploy` 会失败（免费版必须是 SQLite-backed 类）：

```toml
[[durable_objects.bindings]]
name = "RATE_LIMITER"
class_name = "RateLimiter"

# 【补正】免费版 DO 必须声明为 SQLite-backed
[[migrations]]
tag = "v1"
new_sqlite_classes = ["RateLimiter"]
```

---

## §A9 API 路由清单（v1）

| Method | Path | 鉴权 | 限流 | 缓存 | 说明 |
| --- | --- | --- | --- | --- | --- |
| POST | `/api/v1/auth/login` | — | ✅双桶 | — | 签发 Cookie |
| POST | `/api/v1/auth/logout` | ✅ | — | — | 清 Cookie |
| GET | `/api/v1/topics` | — | — | L2 300s | 主题列表 + 模块开关 |
| GET | `/api/v1/topics/:id/chapters` | — | — | L2 300s | 章节树 |
| GET | `/api/v1/chapters/:id` | — | — | L2 300s | MD 正文（客户端渲染） |
| GET | `/api/v1/quiz/:chapterId` | — | — | L2 120s | 题面（**不含答案**） |
| POST | `/api/v1/quiz/submit` | — | ✅ | — | 判题 + 错题本写入，幂等 |
| GET | `/api/v1/sql/exercises/:id` | — | — | L2 300s | 题面 + 数据集 JSON |
| POST | `/api/v1/sql/result` | — | ✅ | — | 只上报结果与耗时（§4.1） |
| GET | `/api/v1/practice/solutions/:id` | — | — | L2 300s | 积木容错规则 |
| POST | `/api/v1/practice/record` | — | ✅ | — | 排障记录存档 |
| POST | `/api/v1/progress/batch` | — | ✅ | — | 批量事件，幂等 |
| GET | `/api/v1/progress/summary` | — | — | — | 读 `stats_daily` |
| GET | `/api/v1/admin/*` | ✅ | — | — | 后台读 |
| POST/PUT/DELETE | `/api/v1/admin/*` | ✅ | ✅ | — | 后台写，校验 Origin |
| POST | `/api/v1/admin/import/chunk` | ✅ | ✅ | — | Excel 分片 |
| POST | `/api/v1/admin/import/commit` | ✅ | ✅ | — | 提交导入 |
| GET | `/api/v1/admin/export` | ✅ | ✅ | — | 全量 JSON 导出（§25） |

**注**：学员端固定 `user_id='me'`（§8），因此学员接口无鉴权但**必须限流**，否则等于开放写入口。

---

## §A10 可观测性与 SLO

### A10.1 结构化日志字段（统一 schema）

```json
{ "ts": 1753934400000, "lvl": "info", "msg": "req",
  "traceId": "...", "path": "/api/v1/quiz/submit", "method": "POST",
  "status": 200, "durationMs": 42, "d1Stmts": 3, "cache": "miss",
  "userId": "me", "degrade": "L0" }
```

`d1Stmts` 与 `degrade` 是本方案新增字段：前者用于事后审计"哪个接口在逼近 40 条预算"，后者用于统计降级发生频率。

### A10.2 关键告警埋点（§25 基础上补充）

| 事件 | 级别 | 阈值 |
| --- | --- | --- |
| 登录失败 | warn | 单账号 5 次/分钟 |
| 限流触发 | warn | 任意 |
| D1 慢查询 | warn | > 100ms |
| 查询预算超限（5002） | **error** | 任意一次即需修代码 |
| 进入 L1/L2 降级 | **error** | 任意 |
| 日请求数 | info | 每日 > 6 万（60% 额度）需关注 |

### A10.3 SLO

| 指标 | 目标 | 说明 |
| --- | --- | --- |
| API 可用性 | ≥ 99.5%/月 | 免费套餐不承诺更高，诚实设定 |
| 只读接口 p95 | < 120ms | 缓存命中路径应 < 30ms |
| 写接口 p95 | < 250ms | 含 D1 往返 |
| Worker CPU p99 | < 8ms | 逼近 10ms 即触发优化 |
| 单请求 D1 语句数 p99 | ≤ 10 | 硬上限 40 |

---

## §A11 演进路线与扩容触发点

### A11.1 落地顺序（对齐规范 §7 / §10）

| 阶段 | 后端交付物 | 验收标准 |
| --- | --- | --- |
| **0** | `core/` + `middleware/` + `DbSession` + migrations + CI | 一条 `/api/v1/health` 走完整管道，日志含 traceId 与 d1Stmts |
| **0** | `RateLimiter` DO + 登录 + 会话 | 暴力破解 6 次即被锁定，DO 请求数可观测 |
| **0.5** | content 只读链路 + Cache 层 + progress/batch | 竖切 demo：1 主题 → 1 章 → 3 题 → 1 条进度，缓存命中率 > 80% |
| **1** | admin CRUD + Excel 分片导入 + 导出 | 1000 行 Excel 导入零重复、可断点重传 |
| **2** | quiz 判题 + 错题本 | 提交幂等，重复提交返回首次结果 |
| **3** | practice 记录 + solutions 下发 | — |
| **4** | stats_daily 写时聚合 + summary | 仪表盘零 GROUP BY |

### A11.2 扩容触发点（何时必须离开免费版）

明确写下来，避免"到时候慌"：

| 信号 | 含义 | 动作 |
| --- | --- | --- |
| 日请求 > 8 万 | Workers 额度 80% | 加强缓存 TTL；仍不够则升 $5/月 Workers Paid |
| D1 库 > 350MB | 500MB 的 70% | 归档历史 `progress_events`（保留 90 天）到导出文件 |
| Worker CPU p99 > 9ms | 10ms 逼近 | 检查是否有逻辑该下沉客户端；无法优化则升级付费版 |
| DO 请求 > 7 万/天 | DO 额度 70% | 收窄限流挂载端点 |
| 慢查询 > 100ms 频发 | D1 排队 | `EXPLAIN QUERY PLAN` 补索引；拆大表 |

**架构上已为付费升级留好口子**：升级后只需改三处——`MAX_STMT_PER_REQUEST` 40→800、Markdown 可选改为发布时服务端预渲染落库（§15.2）、`SLOW_QUERY_MS` 阈值。**没有任何架构性重写**，这就是"可扩展"的实际含义。

---

## §A12 风险登记表

| # | 风险 | 影响 | 缓解 | 残余风险 |
| --- | --- | --- | --- | --- |
| R1 | D1 单库单线程，一条全表扫描阻塞全站 | 高 | 预算守卫 + CI EXPLAIN 校验 + 慢查询告警 + L1 降级 | 中→低 |
| R2 | 学员端接口无鉴权被刷 | 中 | DO 令牌桶 + WAF 兜底 + 写额度守恒 | 低 |
| R3 | token_version 60s 缓存窗口 | 低 | 单管理员场景可接受 | 低（已记演进项） |
| R4 | 单飞仅 isolate 内有效，跨节点仍可能击穿 | 低 | 个人项目流量下不成立 | 低 |
| R5 | Excel 导入中断留下半份题库 | 中 | staging → commit 两阶段 + 分片幂等 | 低 |
| R6 | 缓存的题面接口误带答案 | **高** | 接口 DTO 显式白名单字段 + 单测断言响应不含 `answer` 字段 | 低 |
| R7 | 免费额度被单日爬虫打满 | 中 | WAF `/api/*` 规则 + 缓存 + 额度告警 | 中（免费版无法根治） |
| R8 | DO 与 Worker 额度双重消耗 | 中 | 限流仅挂敏感端点 | 低 |

**R6 单独说明**：这是本方案里唯一被评为"高影响 + 易发生"的风险。缓存会把泄露固化 300 秒，且难以察觉。因此强制要求：所有下发给学员端的题目 DTO 必须由显式字段白名单构造（`pick(row, ['id','stem','options'])`），**禁止 `SELECT *` 后直接 `JSON.stringify(row)`**，并在 §21 单测中加一条断言。

---

## §A13 与 v2 规范的差异与补正清单

本方案**未改变任何 v2 已拍板决议**，仅做补正与细化：

| 类型 | 内容 | 对应章节 |
| --- | --- | --- |
| **补正（规范缺失，不补会出错）** | `wrangler.toml` 缺 `[[migrations]] new_sqlite_classes` | §A8.4 |
| **补正** | Excel 导入缺跨请求整体一致性方案 | §A7.4 |
| **补正** | `token_version` 每请求查 D1 的额度问题未考虑 | §A8.2 |
| **增量** | Cache API 只读缓存层（规范完全未提，最高杠杆） | §A6 |
| **增量** | 查询预算运行时守卫（把设计约束变成代码护栏） | §A4.1 |
| **增量** | 三级降级与只读 stale 返回 | §A7.1 |
| **增量** | 幂等性矩阵 | §A7.3 |
| **增量** | demo config 白名单的具体 schema 机制与"写时校验"决策 | §A5.3 |
| **增量** | 错误码分区表、SLO、扩容触发点 | §A3.3 / §A10.3 / §A11.2 |

如需将上述补正回写为《生产规范 v3》，建议只回写三条**补正**类（会导致部署失败或数据不一致的），增量类留在本架构文档即可，避免规范文档膨胀失去可读性。
